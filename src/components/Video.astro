---
interface Props {
  src: string;
  muted?: boolean;
  loop?: boolean;
}

const { src, muted = true, loop = false } = Astro.props;
---

<video
  width="720"
  controls
  loop={loop}
  muted={muted}
  controlslist="nodownload disablepictureinpicture"
  preload="metadata"
  playsinline
>
  <source src={src + "#t=0.001"} type="video/mp4" />
</video>
